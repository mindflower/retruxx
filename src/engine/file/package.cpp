#include <file/package.h>

namespace m3d
{
    namespace fs
    {
        void Package::GetOpenFilesList(std::vector<CStr>& fileList) const
        {
            fileList.clear();
            //TODO: thats all?
        }
    }
}
